# v0.8 ledger — explicit credit transport

Canonical execution: GitHub Actions run `31210731500`, Python 3.12, exact repository v0.7 subclass.

## Question

Does a physical return path for soma consequence repair the weak delayed credit seen in v0.7?

## Hard controls

- exact v0.7 wave / extracellular solve / persistent growth inherited;
- all free-learning arms use coherent extracellular guidance;
- binary fixed-speed anatomy preserved;
- final mass restored;
- newborn grace = 18 ticks for every arm so delayed carriers have causal opportunity to arrive.

## Carrier calibration

- retrograde: 69 distal cells, `corr(distance, latency)=1.0`, slope `1 tick/edge`;
- scrambled retrograde: same latency law;
- trophic: 49 cells, correlation `.9486`, slope `1.832 ticks/edge`;
- global: all 70 cells after fixed delay.

## Known-tag positive control, 128 paired trials

Tagged side-branch survival:

- positive: `.9875`;
- zero: `.9391`;
- negative: `.6328`.

Positive-negative mean `+.3547`; zero-negative `+.3063`. No Monte-Carlo sign-flip exceedance in 50,000 draws for either.

## Free screen, 8 paired seeds

Final contrast means:

- global `+.0645`;
- retrograde `-.0221`;
- trophic `-.0259`;
- hybrid `+.0021`;
- scrambled retrograde `+.0038`;
- no credit `-.0259`.

No carrier significantly beats no credit. Global-no credit `+.0904`, p=`.2891`. Retrograde-no credit `+.0038`, p=`.9688`.

## Retrograde confirmation, 16 paired seeds

Retrograde-no credit:

- path `+.500`, p=`.2500`;
- edge50 `+2.063` frames, p=`.4839`;
- common25 `+2.063`, p=`.3842`;
- contrast `-.02510`, p=`.66394`.

Verdict: null.

## Interpretation

Transport is not the bottleneck by itself. Carrier physics works and correctly tagged anatomy is selectable. Natural activity/structural eligibility does not yet identify the structural event that causally improved soma performance.

## Status

- `[V]` graph-retrograde carrier;
- `[V]` diffusible trophic carrier;
- `[V]` known-tag branch survival responds to delayed credit;
- `[K]` physical return path alone repairs v0.7 credit;
- `[K]` retrograde carrier alone is sufficient;
- `[~]` biological mapping.
